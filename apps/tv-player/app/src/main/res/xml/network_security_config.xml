<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- LAN HTTP during install. Production CMS should be HTTPS (Phase 14). -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
